<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HandypageApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Handypage">
        <!-- adjustResize (not the adjustUnspecified default): with
             edge-to-edge the unspecified mode pans the window on this ROM,
             and the pan plus ChatPanel's imePadding() double-lifts the chat
             input a full IME-height above the keyboard. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ReaderActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".paper.PaperReaderActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SettingsActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
